object Solution {
    def summaryRanges(nums: Array[Int]): List[String] = {
        if (nums.length == 0) {
            return List()
        }
        var result = Array()
        var rangeStart = 
        nums.map(num => {
            var beginning = num
            // if the next num = current + 1
                // continue current range
            // else
                // close it
        })
        return result
    }
}